eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8801/eureka/
server:
  port: 8814
spring:
  application:
    name: ribbon-user-consumer
