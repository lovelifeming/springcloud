eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8801/eureka/
server:
  port: 8815
spring:
  application:
    name: feign-user-consumer
